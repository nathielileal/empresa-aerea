spring:
  datasource:
    url: jdbc:postgresql://${SPRING_DATASOURCE_HOST:msreserva-db}:${SPRING_DATASOURCE_PORT:5432}/${SPRING_DATASOURCE_DATABASE:reserva_db}
    username: ${SPRING_DATASOURCE_USERNAME:reserva_user}
    password: ${SPRING_DATASOURCE_PASSWORD:reserva_pass}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: msreserva
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

server:
  port: 8084
