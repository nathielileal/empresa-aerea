spring:
  application:
    name: voo-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dbdevelopment:5432/postgres
    username: emiratads_voo_user
    password: senha3
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        default_schema: emiratads_voo
  rabbitmq:
    host: rabbitmq
    username: guest
    password: guest
gateway:
  url: ${GATEWAY_URL:http://localhost:3030}